{
  "name": "s3-proxy-server",
  "version": "1.0.0",
  "description": "Authenticate and proxy web requests to an S3 bucket",
  "main": "server.js",
  "engines": {
    "node": "6.x"
  },
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gregor MacLennan",
  "license": "MIT",
  "dependencies": {
    "aws4": "^1.4.1",
    "basic-auth": "^1.0.4",
    "express": "^4.13.4",
    "awscred": "^1.4.2"
  },
  "devDependencies": {
    "dotenv": "^2.0.0"
  }
}
